// "Set active provider" flow stub
import type { ConfigService } from '../../config/config.service.js';

export async function runSetActiveFlow(configService: ConfigService): Promise<void> {
  // Implementation pending
  void configService;
}
