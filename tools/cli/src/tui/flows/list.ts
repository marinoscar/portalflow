// "List providers" display flow stub
import type { ConfigService } from '../../config/config.service.js';

export async function runListFlow(configService: ConfigService): Promise<void> {
  // Implementation pending
  void configService;
}
