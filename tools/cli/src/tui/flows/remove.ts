// "Remove a provider" flow stub
import type { ConfigService } from '../../config/config.service.js';

export async function runRemoveFlow(configService: ConfigService): Promise<void> {
  // Implementation pending
  void configService;
}
